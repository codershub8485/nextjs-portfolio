# Next.js Portfolio - Shubham Pawar

This is a modern Next.js portfolio template (App Router) with Tailwind CSS and Framer Motion animations.

## Quick start

1. Install: `npm install`
2. Dev: `npm run dev`
3. Build: `npm run build`

Add your images under `public/images/` and update `lib/projects.js`.
